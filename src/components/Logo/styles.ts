import { styled } from "styled-components";
import { clr } from "../../globalStyle";

export const Wrapper = styled.div `
  a {
    color: ${clr.title};
    letter-spacing: -3px;
    font-size: 1.75rem;
    font-weight: 800;
  }
`