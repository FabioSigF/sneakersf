import { styled } from "styled-components";
import { clr } from "../../../globalStyle";

export const Wrapper = styled.div`
  border-top: 1px solid ${clr.grayText};
  color: ${clr.whiteText};
  padding-top: 1.5rem;
  padding-bottom: 1.5rem;
`;
export const Content = styled.div`
  width: 100%;
  text-align: center;
`;
